name: Frontend Workshop Demo

on:
  push:
    branches:
      - 'feature/**'
      - 'bugfix/**'
      - 'chore/**'
      - main # อย่าลืมลบ ถ้าใช้ flow ปกติ

jobs:
  job-1:
    runs-on: ubuntu-latest
    environment: development
    steps:
      - run: echo "Hello Frontend App"
